# 2026-04-23 - La Cosecha del Precedente

> El día en que todo lo sembrado el miércoles llegó a main: el fix de `DetalleRetardo` y la refactorización de agentes mergeados, y la corrección del bug latente en `IntervaloTemporal` en proceso.

## Lo que se logró

**PR #144 mergeado — #114 cerrado: `DetalleRetardo` con todas las correcciones en main (08:22)**
El VO del desglose llegó finalmente a main en su forma correcta: `sealed class` con `IEquatable<T>` manual usando `SequenceEqual`, ctor privado vacío, `ConfigurarSerializacion` registrada en `ConfiguracionSerializacionControlHoras`, y tests de round-trip usando `CrearOpcionesMarten()` con barrera "sin registro falla". La regla de dominio adicional detectada en la segunda ronda de review también viajó: el castigo del retardo satura en cero cuando la compensación excede, y los datos intermedios del VO son privados — la interfaz pública quedó reducida a `Crear`, `Vacio`, `RetardoNeto` y `ToString()`.

**PR #146 mergeado — #145 cerrado: refactorización de agentes formalizada en main (08:53)**
El principio "los ADRs son la única fuente de verdad arquitectónica — los agentes consultan, no duplican" quedó formalizado. CLAUDE.md con índice temático de ADRs, planner con sección `## ADRs aplicables` obligatoria, implementer y reviewer depurados de las reglas que replicaban ADRs.

**`planner.md` actualizado: distinguir valor observable de dato intermedio (08:21)**
La lección del review de `DetalleRetardo` se absorbió en el template del planner: antes de listar una propiedad como pública, el planner debe preguntar si es un valor observable externamente o un dato intermedio. Referencia a ADR-0015 "Tell Don't Ask".

**Issue #147 cerrado: auditoría de callers de `DetalleRetardo` completada (13:42)**
El tooling verificó que ningún caller externo accede a los campos privatizados. La encapsulación formal quedó verificada tanto por el compilador como por la auditoría.

**Pipeline issue #143 completado — PR #148 abierto: `IntervaloTemporal` alineado con ADR-0015**
El bug latente identificado el miércoles — `[JsonConstructor]` en `IntervaloTemporal` más ausencia de registro en Marten — fue atacado con el pipeline TDD completo:
- Fase roja: tests para serialización Marten, factories de conversión, barrera "sin registro falla"
- Fase verde: `Desde(DateTime, DateOnly)`, `DesdeMinutosAbsolutos(int)`, `Parte(int)` implementados; `ConfigurarSerializacion` registrada
- Refactor: 47 tests de round-trip con STJ vanilla eliminados (reemplazados por tests que ejercitan el contrato de producción)

PR #148 creado. Queda pendiente de revisión y merge.

## Problemas encontrados

No hubo problemas técnicos nuevos el 23. El trabajo fue ejecución limpia de lo planificado el día anterior. El único punto abierto al final del día: PR #148 en revisión, `IntervaloTemporal` sin llegar a main todavía.

## Lo que descartamos

Ninguna alternativa notable fue considerada y descartada el 23 — fue un día de ejecución, no de decisión.

## Aprendizajes

1. **El cierre de un ciclo largo se siente diferente cuando el bug era sistémico.** Mergear PR #144 no fue solo "merge el feature" — fue validar que las tres capas de corrección (arquitectura de agentes, reglas de dominio, tests con contrato real) se sostenían juntas.

2. **Los tests obsoletos son deuda visible.** Eliminar los 47 tests de round-trip vanilla en el refactor de #143 fue más que limpieza: fue admitir que esos tests habían dado una falsa sensación de seguridad. Reemplazarlos por `IntervaloTemporalSerializacionMartenTests` con `CrearOpcionesMarten()` convierte la barrera de seguridad en real.

## Números del día

| Metrica | Valor |
|---|---|
| Commits | 9 |
| PRs mergeados | 2 (#144, #146) |
| PRs abiertos al final del día | 1 (#148) |
| Issues cerrados | 3 (#114, #145, #147) |
| Archivos cambiados | 17 |
| Lineas agregadas | ~405 |
| Tests eliminados (obsoletos) | 47 |
