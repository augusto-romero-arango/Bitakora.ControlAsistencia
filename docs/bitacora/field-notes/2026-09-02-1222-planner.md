---
fecha: 2026-09-02
hora: 12:22
sesion: planner
tema: revision del backlog, oleadas y refinamiento de #576 y #551
---

## Contexto
El usuario pidio saber que esta listo para implementar, en que orden, y que falta refinar. Diez issues abiertos: cinco listos en cadena (MCP Comandos), tres borradores, dos borradores aparcados.

## Descubrimientos
- Cadena estricta del MCP de Comandos: #575 -> #571 -> #572 -> #573 -> #574. Todos los de codigo comparten `ConfiguracionClientesHttp.cs`, `ComposicionDelServidorTests.cs` y la suite smoke: /sequential.
- `deploy-mcp-comandos.yml` corria en el momento de la sesion (primer deploy tras #570); el CA-4 de #570 (system key `mcp_extension`) es el gate real de #571.
- `WORKOS_AUTHORIZATION_SERVER_URL` no existe aun como GitHub variable (solo `WORKOS_CLIENT_ID`): paso manual previo a mergear #575.
- `.claude/pipeline/.plugin-root` apunta a 0.34.0 mientras 0.35.0 ya esta en cache; #575 y #551 convergen a scaffolders de 0.35.0.
- El 401 app-side de `Mcp.Consultas` (`AutorizacionMcpMiddleware`) no es alcanzable hoy: las tools llegan por McpToolTrigger sin HttpContext y las Functions HTTP restantes estan exentas. El `resource_metadata` que emite es coherencia, no seguridad.
- El daemon de Marten no "reconstruye" al recrear el Container App: reanuda desde `mt_event_progression`. El costo del rename es una ventana de lag, no un replay.
- `deploy-projections.yml` se autodispara por push cuando cambia el propio workflow: carrera con Infra CD al mergear #551 (esperada; relanzar por workflow_dispatch).

## Decisiones
- #576: derivar la URL del PRM desde `Mcp__ResourceUri` (recurso menos ultimo segmento + `/.well-known/oauth-protected-resource/` + ultimo segmento). Descartado `Mcp__PrmUri`: reabriria #575 y en HCL formaria ciclo modulo APIM <-> Function App. Fail-fast si el recurso no tiene segmento de ruta. Titulo renombrado. Sigue `bloqueado` por #575.
- #551: solo el Container App (`ca-projections-asist-dev`, 24 chars -- el borrador decia 22). Identidad y environment fuera de alcance. Sin introducir `prefix_func` en `variables.tf` (renombraria recursos desplegados, MEF-ADR-0045 s.3). Labels dom: los cuatro dominios (continente del read-side).
- Oleadas propuestas: O1 #575 + #551; O2 #571 + #576; O3 #572; O4 #573; O5 #574.

## Descartado
- Alinear el middleware de Consultas al scaffold 0.35.0 (nunca bloquear, retirar `UriMetadataRecursoProtegido`): decision de diseno aparte, registrada como alternativa en #576, no colada en el refactor.
- Encadenar `deploy-projections.yml` tras Infra CD via `workflow_run` dentro de #551: scope creep.

## Preguntas abiertas
- #358 (22 dias en borrador, sin tipo): agendar sesion de analisis/event-storming o cerrar not planned.
- #537 (M2M dispositivos): decision de producto -- hay dispositivos en el horizonte?
- Si el usuario quiere tomar la alternativa canonica del middleware de Consultas, #576 queda superado.

## Referencias
Issues refinados: #576 (estado:listo, bloqueado por #575), #551 (estado:listo).
Issues creados: ninguno. Issues cerrados: ninguno.
