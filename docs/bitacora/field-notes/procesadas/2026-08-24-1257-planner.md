---
fecha: 2026-08-24
hora: 12:57
sesion: planner
tema: Refinamiento de #426 -- read model AsistenciaDiaria con su proyeccion
---

## Contexto
El usuario pidio refinar el issue #426 (borrador de la sesion 2026-08-17, bloqueado por #425). Es la vista de decision del Aprobador: fila liviana por colaborador+dia, proyeccion N1 sobre el stream `dc:`.

## Descubrimientos
- El borrador arrastraba la notacion vieja `dc_...` del stream; #425 (refinado 2026-08-24) fijo `dc:{CodigoColaborador}:{yyyyMMdd}` con separador `:`. Corregido. Nota lateral: con `:` el id ya NO es URL-safe (RFC 3986) -- la premisa "URL-safe por diseno" que #429 hereda del borrador quedo obsoleta; lo resolvera #429 con su ruta.
- `UltimaDepuracion` rastreado hasta la sesion 2026-08-17: quedo sembrado en glosario, #426 y #429 sin ninguna decision del Aprobador que lo justifique -- no paso el filtro "¿que decision habilita?" (MEF-ADR-0041).
- Materia prima verificada en codigo: senal estructural del plan en `DiaDepurado` (NombreTurno null = sin programacion; nombre + franjas vacias = descanso; nombre + franjas >= 1 = jornada), `FranjaDepurada.EsAnomala` explicita, `HorasPorConcepto` ya sparse desde el productor (PR #435).
- Patron completo en el repo: `TurnoVigente`/`TurnoVigenteProjection`/seam/config-test -- #426 es una repeticion disciplinada de ese corte.

## Decisiones
1. **Ratificado**: las 3 banderas crudas del borrador (`SinProgramacion`/`SinMarcaciones`/`FranjasIncompletas`) se reemplazan por los dos ejes del Aprobador -- Plan (`ConJornada`/`Descanso`/`SinProgramar` + `NombreTurno`) y 4 anomalias ya juzgadas (`NoSePresento`, `FranjasIncompletas`, `VinoEnDescanso`, `TrabajoSinProgramacion`). Responde la pregunta abierta: `VinoEnDescanso` es bandera propia. La derivacion vive una sola vez en Create/Apply; #427/#428 leen directo (la fila liviana no carga franjas ni marcaciones para re-derivar).
2. **`UltimaDepuracion` retirado** de la vista y del glosario. Comentario dejado en #429 para que su refinamiento no lo arrastre por inercia.
3. **`NumeroMarcaciones` descartado**: la fila decide "a quien miro"; el contexto de investigacion es #429.
4. #426 pasa a `estado:listo` (revision de complejidad + DoR de projection satisfechos); conserva `bloqueado` (#425 abierto).

## Descartado
- Conservar banderas crudas en la fila: obligaria a #427/#428 a re-derivar anomalias en query-time sin datos.
- Semantica causal para `UltimaDepuracion` (habria tocado #424 cerrado y los dos productores) -- irrelevante tras el retiro del campo.

## Preguntas abiertas
- #429: forma de la ruta del detalle en vivo (id `dc:` ya no es URL-safe; ruta descompuesta `/{codigo}/{fecha}` como candidata) -- pendiente de su refinamiento contra el ADR de #417.
- Si `DepuracionDiaRecibida` nace en #425 con metodo de clasificacion de la senal estructural, la proyeccion de #426 deberia usarlo (Tell-don't-Ask) -- dejado como propuesta revisable en el issue.

## Referencias
Issues tocados: #426 (refinado, borrador -> listo), #429 (comentario sobre UltimaDepuracion).
Glosario: AsistenciaDiaria (forma final en dos ejes, retiro de UltimaDepuracion, notacion dc:).
