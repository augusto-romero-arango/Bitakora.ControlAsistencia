---
fecha: 2026-08-30
hora: 08:42
sesion: planner
tema: Refinamiento final de #502 (servidor MCP de consultas) -- borrador a listo
---

## Contexto
Resolver las preguntas abiertas de #502 tras fijar en la sesion previa que todo el piloto
(#502+#509) se implementa manualmente, fuera de los pipelines de Mefisto.

## Decisiones
- **`consultar_programacion` es UNA sola tool de rango** sobre el QUERY `ListarTurnosVigentes`
  (desde/hasta obligatorios; codigo_colaborador y sede_id opcionales); el caso puntual se cubre
  con desde=hasta. Sin tool aparte sobre `ObtenerTurnoVigente` (tools consolidadas, Anthropic).
- **Respuestas REMODELADAS para token-eficiencia, no passthrough** (el experto primero eligio
  passthrough y se retracto tras la explicacion del remodelado): podar campos internos
  (EtiquetasNormalizadas, centinelas), compactar estructuras (bloques -> "06:00-14:00, sede
  Norte"), truncar con senal. Formas exactas se definen en el laboratorio y se documentan.
- **Tests del remodelado como CA firme** (xUnit + HttpMessageHandler falso, fixtures con JSON
  reales de dev); **test de composicion del host MCP como exploracion documentada** (CA-6):
  no esta verificado que la extension lo permita -- logrado o no, es insumo para harness#761.
- #502 pasa a `estado:listo` (implementacion manual: el label no dispara pipeline; nota
  "No lanzar con /implement" en el cuerpo).

## Descartado
- Passthrough de payloads (eleccion inicial revertida por el experto).
- Dos tools de programacion (rango + dia puntual).

## Preguntas abiertas
- Formas compactas exactas del remodelado por tool: se resuelven durante la implementacion
  manual y quedan documentadas como experiencia.

## Referencias
Issues editados: #502 (decisiones integradas, CAs finales, borrador -> listo).
#509 conserva `bloqueado` hasta que #502 cierre.
Glosario: sin cambios de vocabulario del dominio.
