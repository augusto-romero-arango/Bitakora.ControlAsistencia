---
fecha: 2026-08-30
hora: 08:24
sesion: planner
tema: Refinamiento de #509 (deploy + onboarding del servidor MCP de consultas)
---

## Contexto
Refinar #509 tras el cierre de #508 (Function App `func-asist-dev-mcp-consultas` ya aplicado).

## Descubrimientos
- El reusable `smoke-tests-dominio.yml` verifica el despliegue contra `/api/version` (SHA del
  commit); el proyecto MCP no contempla ese endpoint -> smoke tests del MCP requeririan ademas
  `/api/version` en #502.
- `deploy-programacion.yml` documenta en comentarios el hardening (datos del evento via `env`)
  y las exclusiones deliberadas de paths -- el calco debe leerlos, no copiarlos por simetria.

## Decisiones
- **Sin smoke tests en el piloto** (opcion a): verificacion manual via onboarding; deuda
  declarada (excepcion a MEF-ADR-0013) para un issue futuro si el piloto se consolida.
- **Onboarding como README del proyecto** (`src/.../Mcp.Consultas/README.md`), posible porque:
- **Todo el piloto (#502 + #509) se implementa MANUALMENTE, fuera de los pipelines de Mefisto**
  (decision del experto): el piloto es el laboratorio; la experiencia documentada fluye a
  harness#761 para que `Mcp.Comandos` si nazca generado por Mefisto. No lanzar con `/implement`.
- **Workflow con el patron completo** (opcion a): encadenado tras `Infra CD - Terraform Apply`
  (MEF-ADR-0022), `determinar-alcance`, OIDC. Paths sin ensamblados de eventos (cliente HTTP puro).
- CA-5 de #509: anotar la experiencia del piloto como comentario en harness#761 -- el hito que
  habilita refinar la doctrina en Mefisto.

## Descartado
- Smoke tests + `/api/version` desde el dia uno (inflaria #502/#509 con artefactos inciertos).
- Onboarding en `docs/mcp/` (ya no aplica la restriccion de allowlist al ser implementacion manual).

## Preguntas abiertas
- Las mismas de #502 (contrato de consultar_programacion, forma de payloads): siguen en su cuerpo.

## Referencias
Issues editados: #509 (refinado, borrador -> listo, conserva `bloqueado` por #502),
#502 (nota de implementacion manual reemplaza el framing de pipeline TDD).
Glosario: sin cambios (la sesion no toco vocabulario del dominio).
