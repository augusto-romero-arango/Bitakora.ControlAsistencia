---
fecha: 2026-08-19
hora: 09:32
sesion: planner
tema: refinamiento de #420 (renotacion cd: de ControlDiario) + higiene de notacion en glosario y #425
---

## Contexto
Continuacion de la sesion que refino #419. Entre ambos refinamientos cerro #417 y nacio
CA-ADR-0031 (identidad de streams por store), la autoridad de la heuristica de anatomia.

## Descubrimientos
- groupIds del bus: verificacion exhaustiva (src/ + infra/) -- NO existe ningun
  groupId/SessionId ni cola con sesion hoy. El punto "revisar groupIds" del borrador de #420
  se resolvio por verificacion: nada que cambiar.
- Cambio observable en la API: `TurnoVigente.Id` ES el StreamKey (MEF-ADR-0037 seccion 3) y
  se expone en las respuestas de ObtenerTurnoVigente/ListarTurnosVigentes -- pasara a llevar
  `cd:`. Riesgo aceptado (solo dev, sin consumidores externos), declarado en el issue.
- Distincion critica clave-vs-fecha-de-API documentada en el issue: los `yyyy-MM-dd` de rutas,
  query params y payloads NO cambian; solo las composiciones de stream id.
- `RegistrarMarcacionSmokeTests` compone claves de ControlDiario inline (lineas 309/431):
  archivo tocado por #419 Y #420 -- ratifica la dependencia secuencial declarada.
- Divergencia de notacion en el glosario: la entrada DiaCalculado (boceto 2026-08-17) decia
  "dc_{codigo}_{yyyy-MM-dd}", contradiciendo la anatomia decidida el 2026-08-18. El borrador
  #425 arrastraba la misma notacion en dos lineas.

## Decisiones
- #420: borrador -> `estado:listo` (sigue `bloqueado` por #419). Titulo ajustado: se retiro
  "con migracion y rebuild de TurnoVigente" -- purga manual del experto fuera del pipeline
  (misma decision que #419); con purga, el rebuild de TurnoVigente es gratis (documentos y
  progresion del daemon se van con el store y se rematerializan).
- #419: se retiro el label `bloqueado` (su unica dependencia, #417, cerro hoy).
- Glosario: corregida la notacion del futuro stream de DiaCalculado a "dc:{codigo}:{yyyyMMdd}"
  (anatomia CA-ADR-0031), con nota de la correccion.
- #425 (borrador): correccion quirurgica de los dos strings "dc_..." a la notacion decidida,
  sin refinamiento completo (queda para su propia sesion).
- Volumen de archivos de #420 (~17) justificado como issue homogeneo: un solo eje (la
  notacion), cambios mecanicos de constantes/literales/comentarios.

## Descartado
- Tocar groupIds o topologia de sesion en #420: no existen hoy; si algun dia se adopta fan-in
  con sesion, el groupId debera ser la clave nueva (nota en el issue).
- Cambiar rutas o formatos de fecha de la API: ortogonales a la clave.

## Preguntas abiertas
- #420 queda listo pero bloqueado hasta que #419 cierre (cadena natural para /sequential:
  #419 -> #420).
- El refinamiento de #425 debera declarar el riesgo de convergencia MEF-ADR-0026 sobre el
  stream dc: (dos handlers publican DiaDepurado) -- la topologia la decide el pipeline.

## Referencias
Issues refinados: #420 (borrador -> listo, bloqueado por #419)
Issues tocados: #419 (desbloqueado), #425 (correccion de notacion, sigue borrador)
