---
fecha: 2026-08-30
hora: 22:04
sesion: planner
tema: cierre de la cadena colaborador+sede en el read-side (#519) y tool MCP listar_colaboradores (#530)
---

## Contexto
Revision del backlog en estado:listo y refinamiento de #519 (proyectar la sede en la ficha),
borrador que quedo de la sesion de #465. Durante la sesion se verifico que #465 y #520 ya
habian mergeado (PRs #525/#526, hoy) -- la dependencia de #519 vencio sola.

## Descubrimientos
- El backlog listo quedo en dos cadenas independientes: #465→#520 (colaboradores, ya cerrada)
  y #514→#415→#413 (infra/tooling, secuencial por conflicto en monitoring/main.tf).
- `/sequential` solo soporta pipelines tdd|tooling: los tipo:infra van por `/infra` uno a uno.
- El QUERY ListarFichasColaborador absorbe un campo plano nuevo sin cambio de contrato
  (MEF-ADR-0042): la pregunta abierta 2 de #519 se resolvio leyendo el codigo, sin decision nueva.
- Riesgo detectado y anotado en #519: eventos SedeAsignada ya persistidos en dev antes del
  deploy del Apply nuevo no se reflejan retroactivamente (high-water mark del daemon) --
  rebuild/ProjectionVersion como propuesta revisable del implementer.

## Decisiones
- #519, pregunta cross-store: opcion (a) -- la ficha expone SOLO CodigoSede; el nombre lo
  resuelve el consumidor contra FichaSede. La opcion (b) (evento privado de Sedes) descartada:
  reintroduce la sincronizacion que #465 evito.
- Filtro por sede: un solo codigo, igualdad plana (sin lista -- no hay caso).
- #530 (tool MCP): tool consolidada (identificacion? enruta al GET puntual, sin tool aparte);
  fecha_referencia? default "hoy" America/Bogota resuelto por la tool; SIN enriquecimiento del
  nombre de sede -- sin evidencia de la necesidad (MEF-ADR-0018), incremento futuro si aparece.

## Descartado
- Materializar el nombre de la sede en la ficha (opcion b).
- Lista de sedes en el filtro del QUERY.
- Enriquecimiento del nombre de sede en la tool MCP.
- Tool puntual separada obtener_colaborador.

## Preguntas abiertas
- Comportamiento exacto de la tool cuando identificacion viene junto a otros filtros
  (propuesta: se ignoran y la descripcion lo declara) -- propuesta revisable del implementer.
- Extender la suite smoke e2e del MCP (#516) con la tool nueva: a juicio del implementer/reviewer.

## Referencias
Issues creados: #530 (listo+bloqueado por #519)
Issues refinados: #519 (borrador → listo)
Orden sugerido: /implement 519 → deploy dev → /implement 530; en paralelo /infra 514 → 415 → 413
