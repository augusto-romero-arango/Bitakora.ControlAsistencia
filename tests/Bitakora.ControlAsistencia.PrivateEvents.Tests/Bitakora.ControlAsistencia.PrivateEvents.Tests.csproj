<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Exe</OutputType>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Cosmos.EventSourcing.Testing.Utilities" Version="2.3.1" />
    <PackageReference Include="xunit.v3.mtp-v2" Version="3.*" />
  </ItemGroup>

  <!-- Issue #237: referencia solo a PrivateEvents (que arrastra PublicEvents, dependencia legitima
       del grafo). La barrera aqui es que no alcance ningun DomainEvents ni Function App: lo que se
       publica al bus interno del BC tiene que ser plano y portable por si mismo (MEF-ADR-0023). -->
  <ItemGroup>
    <ProjectReference Include="..\..\src\Bitakora.ControlAsistencia.PrivateEvents\Bitakora.ControlAsistencia.PrivateEvents.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

</Project>
